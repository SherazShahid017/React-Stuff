import React from 'react';

const AnotherMile = (props) => {

    return(
        <>
            <button onClick={props.incremented}>I took another step </button>
        </>
    );
}

export default AnotherMile;