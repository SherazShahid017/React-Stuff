import React from 'react';

const UpdateClock = (props) => {

    
    return(
        <>
            <button onClick={props.updated}> Click Me To Update </button>
        </>
    );
}

export default UpdateClock;